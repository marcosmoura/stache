import { css } from '@linaria/core';

export const spaces = css`
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;

  display: grid;
  grid-auto-flow: column;
  column-gap: 4px;
  align-items: center;
`;
